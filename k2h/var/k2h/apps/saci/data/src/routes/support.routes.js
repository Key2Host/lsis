const express = require('express');
const { postContactform } = require('../controllers/support/contactform');
const router = express.Router();

//router.post('/contactform', postContactform);

module.exports = router;